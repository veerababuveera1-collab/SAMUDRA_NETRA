# OIMS V4 — SAMUDRA NETRA X
# Streamlit Cloud — exact pinned versions (tested compatible)
# Python 3.11 recommended in .streamlit/config.toml

streamlit==1.32.2
plotly==5.20.0
pandas==2.2.1
numpy==1.26.4
folium==0.16.0
streamlit-folium==0.20.0
requests==2.31.0
python-dotenv==1.0.1
paho-mqtt>=1.6.1

